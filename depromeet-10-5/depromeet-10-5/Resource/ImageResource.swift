import UIKit

enum ImageResource {
	static let leftArrow = UIImage(systemName: "arrow.left")
    static let addPhoteFromCamera = UIImage(named: "add_a_photo")
    static let addPhoteFromAlbum = UIImage(named: "add_photo_alternate")
}
